// Public-facing JS: deliberately minimal.
// Livewire v4 already bundles Alpine, so no runtime is needed here.
// Heavy editor dependencies (TipTap) live in resources/js/panel.js
// and are only loaded on authenticated panel pages.
